/* ============================================================
   CoreTab — split application entry

   Runtime code now lives in js/coretab-*.js, grouped by responsibility.
   index.html loads those classic scripts in order to preserve the current
   non-module runtime behavior of the Chrome extension.
   ============================================================ */
